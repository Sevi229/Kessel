$(document).ready(function() {
    // Ihr Code, der ausgeführt wird, sobald der DOM vollständig geladen ist.
    document.getElementById('myDiv').textContent = 'Text, der sofort nach dem Laden des DOM hinzugefügt wird.';
});